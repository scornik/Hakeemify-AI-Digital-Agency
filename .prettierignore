dist
coverage

# Generated by the token compiler. It was previously skipped only as a side effect of the root
# .gitignore's bare `build/` pattern — the same unanchored pattern that was silently excluding
# source files from commits. Now that the ignore is anchored, this needs saying explicitly.
packages/library/build
pnpm-lock.yaml
docs
research
*.md

packages/*/fixtures
